[project]
name = "continuum"
version = "0.1.0"
description = "CONTINUUM — An agentic production-intelligence system for film. Know you have the movie before you leave the set."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
authors = [
    { name = "Benjamin Duske", email = "benduske1979@gmail.com" },
]
keywords = ["film-production", "ai-agents", "gemini", "clickhouse", "google-cloud"]

dependencies = [
    # Google Cloud / Gemini / ADK
    "google-adk>=2.0.0",
    "google-genai>=1.0.0",
    "google-cloud-aiplatform>=1.101.0",

    # ClickHouse (production memory)
    "mcp-clickhouse>=0.1.0",
    "clickhouse-connect>=0.8.0",

    # Parallel (research agent)
    "parallel-web>=0.1.0",

    # Web framework
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "jinja2>=3.1.0",

    # Utilities
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
    "rich>=13.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "ruff>=0.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
